/**
 * Đã hợp nhất vào `PaymentResultPage` (Phase 04-C).
 * Giữ file này chỉ để tương thích ngược với App.tsx (route /payment/:orderId).
 */
export { PaymentResultPage as PaymentPage } from './PaymentResultPage';
