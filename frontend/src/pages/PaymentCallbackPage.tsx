/**
 * Đã hợp nhất vào `PaymentResultPage` (Phase 04-C).
 * Giữ file này chỉ để tương thích ngược với App.tsx (routes /payment/callback, /payment/*-return).
 */
export { PaymentResultPage as PaymentCallbackPage } from './PaymentResultPage';
